---
phase: 05-analytics-settings-pwa-polish
plan: 05
type: execute
wave: 4
depends_on: [05-04]
files_modified: []
autonomous: false
requirements: [PWA-02, ANLX-01, ANLX-02, ANLX-03, ANLX-04, SETT-01, SETT-02, SETT-03]

must_haves:
  truths:
    - "Dashboard and transaction logging work with network set to Offline in DevTools"
    - "Analytics charts render correctly for the current month"
    - "Settings page shows pre-filled budget values and Save works"
    - "Export downloads a valid JSON file"
    - "Import restores data and shows success message"
    - "Service worker is registered and shown as active in DevTools Application panel"
  artifacts:
    - path: "public/sw.js"
      provides: "Serwist-generated service worker — registered and active in browser"
      contains: "serwist"
  key_links:
    - from: "Browser DevTools > Application > Service Workers"
      to: "public/sw.js"
      via: "ServiceWorkerRegistration from Phase 1 layout.tsx"
      pattern: "sw.js"
---

<objective>
Human verification checkpoint for Phase 5. Claude automates the build and serves the app; the user verifies offline behavior and all new features manually.

Purpose: PWA-02 cannot be verified automatically — it requires a real browser with DevTools network simulation. All other Phase 5 requirements get a final human sign-off pass.
Output: Phase 5 complete and human-approved.
</objective>

<execution_context>
@/Users/genie-dream/.claude/get-shit-done/workflows/execute-plan.md
@/Users/genie-dream/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-analytics-settings-pwa-polish/05-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final automated gate before human verification</name>
  <files></files>
  <action>
    Run the full automated gate to confirm all code is green before asking the user to test:

    1. `npx vitest run --reporter=verbose` — all tests must pass
    2. `npx tsc --noEmit` — no TypeScript errors
    3. `npm run build` — production build must succeed with webpack
    4. Confirm `public/sw.js` exists and is > 1KB: `ls -la public/sw.js`

    If any step fails, fix it before proceeding to the human checkpoint.

    After all steps pass, start a production-like local server so the user can test offline behavior.
    Run `npx next start -p 3001` and report the local URL to the user.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npm run build</automated>
  </verify>
  <done>All tests pass, build succeeds, local server started at http://localhost:3001</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — all Phase 5 features</name>
  <action>Present test scenarios A through E to the user and wait for approval.</action>
  <what-built>
    Phase 5 complete: Analytics charts (donut + bar + summary), Settings with budget editing and JSON export/import, and @serwist/next PWA offline support.
  </what-built>
  <how-to-verify>
    Open http://localhost:3001 (or dev server at http://localhost:3000).

    **Test A — Analytics tab (ANLX-01, ANLX-02, ANLX-03, ANLX-04):**
    1. Tap the Analytics tab in the bottom nav
    2. Confirm a donut chart appears showing spending by category (if you have transactions logged)
    3. Confirm a daily bar chart appears below the donut chart
    4. Confirm a "Monthly Summary" card shows budget, spent, and saved amounts
    5. Tap the left arrow to go to the previous month — charts should update (may show empty state if no prior data)
    6. Tap the right arrow to return to current month

    **Test B — Settings tab (SETT-01):**
    1. Tap the Settings tab
    2. Confirm your income, fixed expenses, and savings goal are pre-filled
    3. Change your monthly income to a different value and tap "Save Changes"
    4. Navigate to the Dashboard tab — confirm the remaining budget and survival budget values updated immediately (no page reload needed)
    5. Return to Settings and revert the income back to the original value

    **Test C — Export/Import (SETT-02, SETT-03):**
    1. On the Settings tab, tap "Export Backup"
    2. Confirm a file named `budgetpulse-backup-YYYY-MM-DD.json` downloads
    3. Open the file — confirm it contains your transactions and budget config as JSON
    4. Tap "Restore Backup" and select the downloaded file
    5. Confirm a dialog asks "This will replace all current data. Are you sure?"
    6. Confirm "OK" — data should be restored (same data, so no visible change)
    7. Confirm success message appears

    **Test D — Offline (PWA-02):**
    1. Open Chrome DevTools (F12)
    2. Go to Network tab — set throttle to "Offline"
    3. Reload the page
    4. Confirm the dashboard still loads (from service worker cache)
    5. Log a new transaction (amount + category + save)
    6. Confirm the transaction appears in history
    7. Navigate between tabs — confirm they all load without network
    8. Set network back to "No throttling"

    **Test E — Service Worker (PWA-02):**
    1. In Chrome DevTools, go to Application → Service Workers
    2. Confirm "sw.js" is listed as Status: activated and running
    3. If it shows "waiting to activate", click "skipWaiting" to activate
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 test scenarios pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Human verification complete when the user types "approved" confirming:
- Analytics charts render and month navigation works
- Settings budget edit saves and dashboard reflects changes immediately
- Export downloads a valid JSON file
- Import validates and restores data
- App works fully offline (service worker active, all core routes cached)
</verification>

<success_criteria>
Phase 5 is done when the user approves all test scenarios A through E. This completes v1 of BudgetPulse — all 30 requirements across all 5 phases are implemented and human-verified.
</success_criteria>

<output>
After completion, create `.planning/phases/05-analytics-settings-pwa-polish/05-05-SUMMARY.md`
</output>
