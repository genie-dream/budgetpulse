import { describe, it } from 'vitest'

describe('Settings: budget update (SETT-01)', () => {
  it.todo('saves updated config to Dexie and syncs Zustand')
})

describe('Settings: JSON export (SETT-02)', () => {
  it.todo('creates downloadable blob')
})

describe('Settings: JSON import (SETT-03)', () => {
  it.todo('validates structure before importing')
})
